language: python
python:
    - "2.6"
    - "2.7"
    - "3.1"
    - "3.2"
    - "3.3"
    - "pypy"
install:
    - pip install -e . --use-mirrors
    - pip install -r test_requirements.pip --use-mirrors
before_script:
    - cd radon
    - make coveralls
